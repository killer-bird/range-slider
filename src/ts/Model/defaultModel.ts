import {ModelOptions} from "../interfaces";

export const defaultSettings: ModelOptions = {
    step: 1,
    min:  -100,
    max: 200,
    from: 20,
    to: 60,
    vertical: false,
    color: '#32a85c',
    scale: true,
    interval: false
}
